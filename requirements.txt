flask>=2.3.0
boto3>=1.34.0
botocore>=1.34.0
requests>=2.31.0
mitmproxy>=10.0.0

# AWS Bedrock Auth UI 依赖
# configparser 是 Python 3 标准库，无需单独安装

# 新增功能依赖
# 网页内容抓取
beautifulsoup4>=4.12.0

# 浏览器自动化 (Playwright)
playwright>=1.40.0

# 上下文管理 (Token 计数)
tiktoken>=0.5.0
